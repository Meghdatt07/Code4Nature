'use client';
import { useMemo, useState } from 'react';
import { ArrowRight, Droplets, Wind } from 'lucide-react';
import { motion } from 'framer-motion';
import { Card } from '@/components/ui/card';
export function PracticeCompare(){const[water,setWater]=useState(50);const methane=useMemo(()=>Math.max(0,100-water*.9),[water]);return <Card className="overflow-hidden"><div className="grid gap-8 lg:grid-cols-[1fr_.8fr] lg:items-center"><div><div className="text-xs tracking-[.18em] text-white/30">INTERACTIVE SCENARIO</div><h3 className="mt-3 text-2xl font-semibold">Move from prolonged flooding toward controlled water management</h3><p className="mt-3 text-sm leading-7 text-white/50">This control is a visual prototype, not a field recommendation. It demonstrates how a scenario parameter can drive downstream model outputs.</p><div className="mt-7"><label className="text-sm font-medium">Scenario intensity: {water}%</label><input aria-label="Scenario intensity" className="mt-4 w-full" type="range" min="0" max="100" value={water} onChange={e=>setWater(Number(e.target.value))}/></div></div><div className="grid gap-3 sm:grid-cols-2"><Mini title="Water management signal" value={`${water}%`} icon={Droplets}/><Mini title="Illustrative methane index" value={`${methane.toFixed(0)} / 100`} icon={Wind}/><div className="sm:col-span-2 rounded-2xl bg-[#8bcfa6]/10 p-5"><div className="text-xs tracking-[.16em] text-[#8bcfa6]">INTERPRETATION</div><div className="mt-2 text-sm leading-7 text-white/70">As the demonstration scenario shifts, the illustrative methane index moves in the opposite direction. Replace this relationship with field-calibrated science later.</div></div></div></div><motion.div className="mt-8 h-3 rounded-full bg-white/5"><motion.div animate={{width:`${100-methane}%`}} className="h-full rounded-full bg-[#8bcfa6]"/></motion.div><div className="mt-4 flex items-center gap-2 text-xs text-white/30">Prototype interaction <ArrowRight size={13}/> deterministic demo output</div></Card>}
function Mini({title,value,icon:Icon}:any){return <div className="rounded-2xl border border-white/10 bg-white/[0.03] p-5"><Icon size={18} className="text-[#8bcfa6]"/><div className="mt-5 text-xs text-white/30">{title}</div><div className="mt-1 text-2xl font-semibold">{value}</div></div>}