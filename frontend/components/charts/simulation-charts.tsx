'use client';
import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,Legend,LineChart,Line,PieChart,Pie,Cell,CartesianGrid} from 'recharts';
import type {Simulation} from '@/types';
import {Card} from '@/components/ui/card';
export function SimulationCharts({r}:{r:Simulation}){const water=[{name:'Conventional',value:r.baseline_water_liters},{name:'Optimized',value:r.project_water_liters}],emissions=[{name:'Baseline',value:r.baseline_emission_total_tco2e},{name:'Project',value:r.project_emission_total_tco2e}],revenue=[{name:'Farmer',value:r.farmer_revenue_usd},{name:'Asterisk',value:r.company_revenue_usd}],prices=[15,20,25].map(p=>({price:`$${p}`,gross:r.potential_credits*p,farmer:r.potential_credits*p*r.farmer_share_percent/100})),area=Array.from({length:6},(_,i)=>Math.max(.5,r.farm_area_ha*(i+1)/6)).map(a=>({area:+a.toFixed(2),reduction:+(a*(r.baseline_emission_tco2e_per_ha-r.project_emission_tco2e_per_ha)).toFixed(2)})),cycle=Array.from({length:10},(_,i)=>({day:i*15+15,water:Math.round(r.baseline_water_liters*(1-(i+1)/10*.05))})),scenarios=[['Conservative',.30,15],['Demo',r.reduction_percent/100,r.carbon_price_usd],['High-reduction',.55,25]].map(([name,red,p])=>({name:String(name),credits:r.farm_area_ha*r.baseline_emission_tco2e_per_ha*Number(red),price:Number(p)}));return <div className="grid gap-5 md:grid-cols-2"><ChartCard title="Conventional vs Optimized Water Use"><ResponsiveContainer><BarChart data={water}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="name" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11} tickFormatter={v=>`${(Number(v)/1e6).toFixed(0)}M`}/><Tooltip formatter={(v:any)=>`${Number(v).toLocaleString()} L`}/><Bar dataKey="value" name="Liters" fill="#8bcfa6" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></ChartCard><ChartCard title="Baseline vs Project Emissions"><ResponsiveContainer><BarChart data={emissions}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="name" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11}/><Tooltip formatter={(v:any)=>`${Number(v).toFixed(2)} tCO₂e`}/><Bar dataKey="value" name="tCO₂e" fill="#84b7cf" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></ChartCard><ChartCard title="Illustrative Revenue Split"><ResponsiveContainer><PieChart><Pie data={revenue} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={85} label>{revenue.map((_,i)=><Cell key={i} fill={['#8bcfa6','#84b7cf'][i]}/>)}</Pie><Tooltip formatter={(v:any)=>`$${Number(v).toFixed(2)}`}/><Legend/></PieChart></ResponsiveContainer></ChartCard><ChartCard title="Carbon Price Sensitivity"><ResponsiveContainer><LineChart data={prices}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="price" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11}/><Tooltip formatter={(v:any)=>`$${Number(v).toFixed(2)}`}/><Line type="monotone" dataKey="gross" stroke="#8bcfa6" strokeWidth={3} dot={{r:4}}/><Line type="monotone" dataKey="farmer" stroke="#84b7cf" strokeWidth={2} dot={{r:3}}/></LineChart></ResponsiveContainer></ChartCard><ChartCard title="Emission Reduction by Farm Area"><ResponsiveContainer><LineChart data={area}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="area" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11}/><Tooltip formatter={(v:any)=>`${Number(v).toFixed(2)} tCO₂e`}/><Line type="monotone" dataKey="reduction" stroke="#8bcfa6" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></ChartCard><ChartCard title="Water Use Over Crop Cycle"><ResponsiveContainer><LineChart data={cycle}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="day" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11}/><Tooltip formatter={(v:any)=>`${Number(v).toLocaleString()} L`}/><Line type="monotone" dataKey="water" stroke="#84b7cf" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></ChartCard><ChartCard title="Scenario Comparison"><ResponsiveContainer><BarChart data={scenarios}><CartesianGrid strokeDasharray="3 3" opacity={.08}/><XAxis dataKey="name" stroke="#89a296" fontSize={11}/><YAxis stroke="#89a296" fontSize={11}/><Tooltip formatter={(v:any)=>Number(v).toFixed(2)}/><Bar dataKey="credits" name="Potential credits" fill="#8bcfa6" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></ChartCard></div>}
function ChartCard({title,children}:{title:string;children:React.ReactNode}){return <Card className="h-[310px]"><div className="mb-3 text-sm font-semibold">{title}</div><div className="h-[250px]">{children}</div></Card>}